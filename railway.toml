[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py check --verbosity=2 && python manage.py migrate && python manage.py runserver 0.0.0.0:$PORT"
healthcheckPath = "/api/health/"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10 