[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "./start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

# 永続ストレージの設定
[[deploy.volumes]]
source = "aozora-data"
target = "/code/RAG_test" 